# 常用提示词
1
```
请用【纯文本】回答，不要使用 Markdown，不要使用标题、编号、项目符号、加粗、引用、分隔线或任何排版结构。请将所有内容写成一段或多段连续的普通文字，像人在聊天窗口里自然打字一样，方便直接复制粘贴。
```
2
```
翻译下列{}中的内容：
```
3
```
你是一名【高级 Python 工程师 + API 结构拆解专家】。

我的目标不是理解原理，而是：
快速掌握这个 Python 包「能用什么、怎么用、先用什么」。

请你把这个包当成一个【工具箱】来拆解。

输入
- Python 包名：{openai}

 输出规则（必须严格遵守）

 Part 1｜功能模块总览（结构拆解）
请将该包拆解为若干【功能模块 / 子系统】  
（例如：IO、数据结构、核心算法、工具函数、配置管理等）

对于每个模块，必须说明：
- 模块名称
- 这个模块在现实代码中是用来干嘛的（一句话）
- 常见调用入口（类 / 函数名）
- 如果要查原始资料，在哪可以找到源头信息
不要编造虚假信息，不要制造幻觉。

 Part 2｜帕累托排序（最重要的 20%）
从上面的模块中，筛选出最常被使用、最有价值的 20% 模块  
按【使用频率 × 实际价值】排序。

 Part 3｜核心模块逐一拆解（必须有代码）
对 Part 2 中的每一个模块，逐一输出：

 对于每个模块，必须包含以下内容：

1️⃣ 这个模块在真实项目中通常用来做什么
（不是抽象定义，而是“你在写什么代码时会用到它”）

2️⃣ 最小可运行示例（MVP）
- 一段能直接 copy 运行的代码
- 不要伪代码
- 不要省略关键步骤

3️⃣ 常见使用方式（1–2 种）
- 常见参数
- 常见组合用法

4️⃣ 一句话使用判断
- “当你遇到 XX 情况，就用这个模块”

 Part 4｜常被忽略但很有用的模块（可选）
列出 2–3 个：
- 初期不常用
- 但一旦用到就很关键的模块

同样给出：
- 用来干嘛
- 最小可运行代码

 输出风格要求（非常重要）
- 不要解释实现原理
- 不要讲历史、不讲设计哲学
- 不要教学语气
- 只讲：是什么 / 干嘛 / 怎么用
- 代码 > 文字
- makdown标题从三级标题开始
```
4
```

```
